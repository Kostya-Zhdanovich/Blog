import styled from "styled-components";


export const StyledTitle = styled('div') `
    & span {
        font-size: 16px;
        font-weight: 400;
        text-decoration: none; 
    }

    & h1 {
        font-size: 56px;
        font-weight: 700;
        text-decoration: none; 
    }
`